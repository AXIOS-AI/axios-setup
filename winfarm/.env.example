# WrenAI Profile — winfarm_firebird
# Copia questo file in `.env` e inserisci i valori reali.
# NON committare mai `.env` con le credenziali reali (escluso dal .gitignore).

TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_RICETTE=
OPENROUTER_API_KEY=
OPENCODE_API_KEY=

FIREBIRD_HOST=localhost
FIREBIRD_DB=/var/lib/firebird/3.0/data/arc2000.fdb
FIREBIRD_USER=SYSDBA
FIREBIRD_PASSWORD=masterkey